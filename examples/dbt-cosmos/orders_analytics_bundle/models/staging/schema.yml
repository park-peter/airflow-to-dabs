version: 2

sources:
  - name: raw
    catalog: main
    schema: analytics
    tables:
      - name: raw_orders

models:
  - name: stg_orders
    description: Deduplicated orders, one row per order_id.
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
